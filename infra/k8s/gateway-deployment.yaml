apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: pncm
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: ilkoninn/pncm-gateway:sha-c2683656cf0446f94186854fb8fb2af2b801c7a7
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          envFrom:
            - configMapRef:
                name: pncm-config
            - secretRef:
                name: pncm-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: pncm-gateway
  namespace: pncm
spec:
  selector:
    app: gateway
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
